# Changelog

## 0.1.0 - Unreleased

- Package md2print as an installable Python CLI.
- Add `compact` and `compact-3ring` print presets.
- Keep metadata banners opt-in with `--meta`.
- Emit self-contained print-ready HTML.
